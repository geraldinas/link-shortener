<% if @shortened_id.present? %>
  <p class='shortened-link'>
    The shortened url is available <%= link_to 'here',
url_url(@shortened_id) %>.
    (Right click and copy link to share it).
  </p>
<% end %>